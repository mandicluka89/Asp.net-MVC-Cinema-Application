@model IEnumerable<BioskopP15.Models.Zanr>

@{
    ViewBag.Title = "Index";
}

<h2>Zanrovi</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Naziv)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Naziv)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ZanrId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ZanrId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ZanrId })
        </td>
    </tr>
}

</table>
